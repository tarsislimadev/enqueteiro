version: '3'

services:

  database:
    hostname: database
    build: './src/database'

  app:
    hostname: app
    build: './src/app'
    volumes:
      - './src/app:/app'
    ports:
      - '8080:80'
